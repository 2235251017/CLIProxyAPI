# CLIProxyAPI 启动文档

本文档用于说明当前项目在本地的基础启动方式，并给出启动后可访问的前端地址。

## 1. 环境准备

- Go 1.26 或更高版本
- 可正常使用的终端环境（Windows PowerShell、cmd、macOS Terminal、Linux shell 均可）

可先确认 Go 版本：

```bash
go version
```

## 2. 准备配置文件

项目默认读取根目录下的 `config.yaml`。

如果你还没有自己的配置文件，可以先基于示例复制一份：

```bash
cp config.example.yaml config.yaml
```

Windows PowerShell 可使用：

```powershell
Copy-Item config.example.yaml config.yaml
```

## 3. 修改最小必要配置

建议至少检查 `config.yaml` 中以下几项：

### 3.1 服务端口

示例配置默认端口为：

```yaml
port: 8317
```

当前仓库里的 `config.yaml` 端口是：

```yaml
port: 14563
```

### 3.2 API Key

至少保留一个可用的客户端 API Key，例如：

```yaml
api-keys:
  - "your-api-key-1"
```

### 3.3 管理端密钥

如果你希望使用项目自带的前端管理页面，必须设置：

```yaml
remote-management:
  secret-key: "your-management-password"
```

如果 `secret-key` 为空，管理相关接口不可用，前端管理页也无法正常完成管理操作。

### 3.4 可选：仅本机访问

如果你只希望本机访问服务，可将：

```yaml
host: "127.0.0.1"
```

这样更适合本地开发或自用场景。

## 4. 启动项目

在项目根目录执行：

```bash
go run ./cmd/server
```

如果你希望先编译再启动，可以执行：

```bash
go build -o cli-proxy-api ./cmd/server
./cli-proxy-api --config config.yaml
```

Windows 下编译后通常为：

```powershell
go build -o cli-proxy-api.exe ./cmd/server
.\cli-proxy-api.exe --config config.yaml
```

## 5. 启动后的访问地址

### 5.1 前端管理页面地址

管理页面路由为：

```text
/management.html
```

因此：

- 使用示例配置默认端口 `8317` 时，前端地址为 `http://127.0.0.1:8317/management.html`
- 按当前仓库里的 `config.yaml` 启动时，前端地址为 `http://127.0.0.1:14563/management.html`

也可以将 `127.0.0.1` 替换为 `localhost`：

- `http://localhost:8317/management.html`
- `http://localhost:14563/management.html`

### 5.2 API 基础地址

启动成功后，OpenAI 兼容接口通常可通过以下前缀访问：

- `http://127.0.0.1:8317/v1`
- 或当前配置对应的 `http://127.0.0.1:14563/v1`

## 6. 常见说明

- `remote-management.secret-key` 不能为空，否则管理接口不可用。
- `remote-management.disable-control-panel` 需要保持为 `false`，否则 `/management.html` 不提供控制面板。
- 如果修改了 `port`，请将文档中的访问地址同步替换为你自己的端口。
- 项目默认会读取工作目录下的 `.env`。

## 7. 推荐的最小启动流程

```powershell
Copy-Item config.example.yaml config.yaml
```

编辑 `config.yaml`，至少设置：

- `api-keys`
- `remote-management.secret-key`
- `port`

然后启动：

```powershell
go run ./cmd/server
```

启动后访问：

- 前端管理页：`http://127.0.0.1:8317/management.html`
- 如果你直接使用当前仓库自带 `config.yaml`：`http://127.0.0.1:14563/management.html`
